micro div42(f) {
    try {
        f()
    }
    .catch {
        case Some(value):
            resume x
        case None:
            resume 42
    }
}

micro main() {
    div42 {
        5 / 0
    }
}

class DivideZeroError {
    numerator: int = 1 + 1
}



micro safe_div(numerator: int, denominator: int) -> int / DivideZeroError {
    if denominator == 0 {
        let a = raise Failure(DivZeroError(numerator))
    }
    else {
        numerator.div_ceil(denominator)
    }
}
